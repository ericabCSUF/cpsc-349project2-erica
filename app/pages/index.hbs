
<!doctype html>
<html>
  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Task Blaster - Sign Up</title>
  </head>
  {{!-- Add name somewhere on this page. I left it out for now since I dont know for sure what we're calling it --}}
  <body>
    {{!-- Nav bar --}}
    <nav class="flex justify-between items-center p-1 px-5 pt-3">
      {{!-- Logo --}}
      <img src="assets/images/Task Blaster Logo transparent.png" class="h-6"alt="Task Blaster Logo" />
      <div class="flex space-x-12 items-center">
        {{!-- Home and about us text --}}
        <a href="#" class="text-xl font-semibold">About Us</a>
      {{!-- Get Started Button --}}
      <button class="bg-blue-400 text-white py-2 px-3 hover:shadow-lg shadow-xl rounded">
        {{#if currentUser}}
        <a href="/user/logout">Logout</a>
        {{else}}
        <a href="/user/signup">Get Started</a>
        {{/if}}

      </button>
      </div>  
    </nav>
    {{!-- Text Section --}}
    <main class="flex justify-center space-x-20 py-20 mt-16 text-left flex-wrap bg-gradient-to-r from-blue-100 to-cyan-100 rounded-lg shadow-lg">
      
      {{!-- Left side --}}
      <div class="flex flex-col items-start space-y-5 text-left">
        <h1 class="text-4xl">Let's help you <span class="text-blue-400 font-bold">organize.</span></h1>
        <p class="text-lg font-semibold pt-6">Modern tracking. Simply efficient.</p>
        <ul class="list-disc pl-4 space-y-6 max-w-sm">
          <li>Task Blaster allows you to easily keep track of all your daily tasks in one simple place.</li>
          <li>Be your very best. Use todo list and never go another day feeling unaccomplished again.</li>
        </ul>
        <p class="text-lg font-semibold">Easy to use.</p>
        <ul class="list-disc pl-4 space-y-6 max-w-sm">
          <li>Add and remove all your daily tasks in one place. And then click one button to add and remove items.</li>
        </ul>
        {{!-- Login & Signup Buttons --}}
        {{#if currentUser}}
        <section>
          <button class="bg-black text-white text-sm justify-center py-2 px-6 hover:shadow-sm shadow-xl rounded">
            <a href="/{{currentUser.details.username}}">Go to your dashboard</a>
          </button>
        </section>
        {{else}}
        <section class="space-x-12">
          <button class="bg-loginBlue text-white text-sm py-2 px-6 hover:shadow-sm shadow-xl rounded">
            <a href="/user/login">Login</a>
          </button>
          <button class="bg-signupBlue text-sm py-2 px-6 hover:shadow-sm shadow-md rounded">
            <a href="/user/signup">Sign up</a>
          </button>
        </section>
        {{/if}}
      </div>
      {{!-- Right side --}}
      <div class="flex flex-col items-center space-y-5 text-left">
        <span>
        <h1 class="text-4xl">Not productive? We can fix that.</h1>
        </span>
        {{!-- Sample image --}}
        <img src="assets/images/Sample image.png" class="shadow-lg rounded-t-md pt-2"alt="Task Blaster Logo" />
      </div>
    </main>
    {{!-- Not sure if this code it supposed to go on the landing page. Does anyone know?
    <ul>
      {{#if currentUser}}
        <li><a href="/{{currentUser.details.username}}">Go to your dashboard</a></li>
      {{else}}
        <li><a href="/user/signup">Sign up for an account</a></li>
      {{/if}}
    </ul>
    --}}
  </body>
</html> 
